---
- name: Configuring Basic Cluster Setup
  hosts: setup_hyperv
  become: no
  gather_facts: yes
  vars:
  vars_prompt:
  pre_tasks:
    - debug:
        msg: 'Staring Configration'

  roles:
    - hyperv
  post_tasks:
    - debug:
        msg: 'Cluster configuration Complete'

