---
- name: Deploying Prism Central 
  hosts: renegade
  become: no
  gather_facts: yes
  vars:
  vars_prompt: 
      
  pre_tasks:
    - debug:
        msg: 'Staring Configration'

  roles:
    - pc-deploy 

  post_tasks:
    - debug:
        msg: 'Cluster configuration Complete'

